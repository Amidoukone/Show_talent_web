import 'package:flutter/material.dart';

import 'video_added_widget.dart';

class VideoManagementWidget extends StatelessWidget {
  const VideoManagementWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const VideoAddedWidget();
  }
}
